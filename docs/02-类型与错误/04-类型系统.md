# 04 — 类型系统

## 是什么

Go 的类型系统以 struct（结构体）、method（方法）、interface（接口）和
embedding（组合）为核心。没有类（class）和继承（inheritance），
用**组合取代继承**。

## 怎么用

### struct 与方法

```go
type User struct {
    ID   int
    Name string
}

// 值接收者：不会修改原对象
func (u User) Greeting() string {
    return fmt.Sprintf("hello, %s", u.Name)
}

// 指针接收者：会修改原对象
func (u *User) SetName(name string) error {
    if u == nil {
        return errors.New("nil receiver")
    }
    u.Name = name
    return nil
}
```

值接收者操作的是副本，指针接收者可以修改原对象。
两种接收者都可以调用，Go 会自动转换。

### 构造函数

Go 没有构造函数的语法关键字，用普通函数代替。

```go
func NewUser(id int, name string) (User, error) {
    if id <= 0 {
        return User{}, ErrInvalidID
    }
    name = strings.TrimSpace(name)
    if name == "" {
        return User{}, ErrEmptyName
    }
    return User{ID: id, Name: name}, nil
}
```

### 接口

Go 的接口是隐式实现（duck typing）：
一个类型只要实现了接口的所有方法，就自动满足该接口，无需 `impl` 关键字。

```go
// 标准库中的 Stringer 接口
type Stringer interface {
    String() string
}

// User 自动实现了 Stringer
func (u User) String() string {
    return fmt.Sprintf("User{ID:%d, Name:%q}", u.ID, u.Name)
}
```

### 组合（Embedding）

Go 用 struct embedding 实现类似继承的效果，
但本质是组合：外层类型"提升"内层的字段和方法。

```go
type Admin struct {
    User          // 嵌入 User，自动获得其字段和方法
    Level int
}

a := Admin{User: User{ID: 1, Name: "alice"}, Level: 10}
a.Name        // 直接访问 User 的字段（被提升）
a.Greeting()  // 直接调用 User 的方法（被提升）
a.IsSuper()   // Admin 自己的方法
```

## 为什么

对比 Rust：

| 特性 | Go | Rust |
|------|----|------|
| 自定义类型 | struct + method | struct + impl |
| 接口 | 隐式实现（duck typing） | 显式 `impl Trait` |
| 组合 | embedding（字段/方法提升） | 无等价语法，手动委托 |
| 继承 | 无 | 无 |
| 泛型 | Go 1.18+ 引入 | 从 1.0 就有，更完善 |

Rust 的 trait 需要显式 `impl`，Go 的 interface 是隐式的——这让
Go 可以方便地为已有类型补充接口实现（比如为 `User` 加 `String()` 方法使其
自动满足 `fmt.Stringer`）。

Go 的 embedding 不是继承。嵌入后无法重写（override）方法。
它是语法糖层面的委托（delegation）。

## 常见坑

1. **指针接收者直接调用导致的 nil panic**：Go 允许用 nil 指针调用方法，
   所以在方法开头要检查 `if u == nil { ... }`。Rust 编译器会阻止这种调用。

2. **值接收者与接口**：如果接口方法用指针接收者实现，值类型变量无法通过
   接口变量调用该方法（需要取地址）。

3. **Embedding 不是继承**：嵌入类型的字段会被"提升"，但不能被"重写"，
   也没有多态分发的效果。

---

对应的代码文件：`internal/04-typesx/typesx.go` / `internal/04-typesx/typesx_test.go`

```
cd /Users/dingchuan/Documents/Repos/GoJuniper && go test ./internal/04-typesx/ -v
```
